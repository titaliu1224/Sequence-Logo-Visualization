{
    "version": 2,
    "builds": [
        {
            "src": "api/(.*)",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index"
        }
    ]
}